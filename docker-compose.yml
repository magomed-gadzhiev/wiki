services:
  db:
    image: postgres:15-alpine
    container_name: wiki_db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-wiki}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: wiki_backend
    environment:
      - DB_ENGINE=django.db.backends.postgresql
      - DB_NAME=${POSTGRES_DB:-wiki}
      - DB_USER=${POSTGRES_USER:-postgres}
      - DB_PASSWORD=${POSTGRES_PASSWORD:-postgres}
      - DB_HOST=db
      - DB_PORT=5432
      - SECRET_KEY=${SECRET_KEY:-django-insecure-change-this-in-production}
      - DEBUG=${DEBUG:-True}
      - DJANGO_SUPERUSER_USERNAME=${DJANGO_SUPERUSER_USERNAME:-admin}
      - DJANGO_SUPERUSER_EMAIL=${DJANGO_SUPERUSER_EMAIL:-admin@example.com}
      - DJANGO_SUPERUSER_PASSWORD=${DJANGO_SUPERUSER_PASSWORD:-admin}
    volumes:
      - ./backend:/app
      - backend_media:/app/media
      - backend_static:/app/staticfiles
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    depends_on:
      db:
        condition: service_healthy
    command: python manage.py runserver 0.0.0.0:8000

  frontend:
    build:
      context: ./frontend
      dockerfile: ${FRONTEND_DOCKERFILE:-Dockerfile.dev}
    container_name: wiki_frontend
    environment:
      - NODE_ENV=${NODE_ENV:-development}
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "${FRONTEND_PORT:-4200}:4205"
    depends_on:
      - backend
    command: npm start

  # kerberos:
  #   image: staticmukesh/kerberos-docker:latest
  #   container_name: wiki_kerberos
  #   environment:
  #     - REALM=${KRB5_REALM:-EXAMPLE.COM}
  #     - DOMAIN_REALM=${KRB5_DOMAIN:-example.com}
  #     - KERB_MASTER_KEY=${KRB5_MASTER_KEY:-MASTERKEY}
  #     - KERB_ADMIN_PASS=${KRB5_ADMIN_PASS:-admin}
  #   ports:
  #     - "88:88"    # Kerberos
  #     - "464:464"  # kadmin
  #     - "749:749"  # Kerberos admin
  #   healthcheck:
  #     test: ["CMD-SHELL", "pgrep -f krb5kdc || exit 1"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # apache:
  #   build:
  #     context: ./apache
  #     dockerfile: Dockerfile
  #   container_name: wiki_apache
  #   ports:
  #     - "${APACHE_PORT:-8080}:80"
  #   volumes:
  #     - ./kerberos/http.keytab:/etc/krb5/http.keytab:ro
  #   depends_on:
  #     - backend
  #     - kerberos
  #   environment:
  #     - KRB5_REALM=${KRB5_REALM:-EXAMPLE.COM}
  #     - KRB5_SERVICE_NAME=${KRB5_SERVICE_NAME:-HTTP/localhost}
  #   healthcheck:
  #     test: ["CMD", "httpd", "-t"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 3

volumes:
  postgres_data:
  backend_media:
  backend_static:

